# Changelog

## 0.7.0

- تثبيت ميزان الميل دون تعديل ملفاته أو منطقه.
- تفعيل وظيفة **تسجيل الجلسات** بمعدلات 1 و2 و4 إطارات في الثانية ومدد 30 و60 و120 ثانية.
- حفظ صورة JPEG وخريطة عمق PNG وبيانات Float32 الخام لكل إطار.
- حفظ Camera Transform وIntrinsics وحالة التتبع وقياسات العمق في `session.json`.
- إضافة عداد للإطارات المحفوظة والمفقودة وحماية من تراكم عمليات الكتابة.
- إضافة مكتبة للجلسات المحفوظة مع التشغيل والمشاركة والحذف.
- إضافة مشغل داخلي للتبديل بين الصورة والعمق والانتقال بين الإطارات وتغيير سرعة العرض.
- تفعيل **مركز التصدير** لصور العمق والغرف والجلسات والتقارير.
- إضافة البحث والتصفية والمشاركة وإعادة التسمية والحذف.
- إضافة إنشاء فهرس شامل بصيغتي JSON وCSV.
- تفعيل ظهور مستندات التطبيق في تطبيق الملفات عبر إعدادات Info.plist.
- تحديث الإصدار إلى `0.7.0` ورقم البناء إلى `8`.

## 0.6.1

- إصلاح فشل بناء Xcode 16.4 في `RoomScanViewModel`.
- إضافة متطلبات `NSCoding` التي يفرضها `RoomCaptureViewDelegate` في iOS SDK 18.5.
- إضافة `@preconcurrency` إلى مطابقة delegate لتفادي تعارض عزل `MainActor` مع بروتوكول RoomPlan.
- تحديث رقم البناء إلى `7`.

## 0.6.0

- تثبيت ميزان الميل على سلوك النسخة 0.4.0 دون تعديل ملفاته.
- تفعيل وظيفة **صورة مع العمق** باستخدام صورة الكاميرا و`sceneDepth` أو `smoothedSceneDepth` من الإطار نفسه.
- حفظ حزمة لكل لقطة تشمل `color.jpg` و`depth-heatmap.png` و`confidence.png` وبيانات Float32 الخام و`metadata.json`.
- إضافة معاينة مباشرة لخريطة العمق، اختيار الخام أو المنعّم، وفلتر الثقة المرتفعة.
- إضافة مشاركة ملفات حزمة الصورة من داخل التطبيق.
- تفعيل وظيفة **مسح الغرفة** باستخدام واجهة `RoomCaptureView` الأصلية من RoomPlan.
- إضافة بدء وإيقاف ومعالجة المسح وعرض أعداد الجدران والأبواب والنوافذ والفتحات والعناصر.
- إضافة تصدير الغرفة إلى JSON وإلى USDZ بصيغتي Parametric وMesh مع المشاركة.
- إضافة فحص دعم RoomPlan داخل معلومات الجهاز.
- إضافة `RoomPlan.framework` للمشروع.
- تحديث الإصدار إلى `0.6.0` والبناء إلى `6`.

## 0.5.0

- تثبيت ميزان الميل على سلوك النسخة 0.4.0 دون تعديل منطقه.
- تفعيل وظيفة **السحابة النقطية** باستخدام عينات Scene Depth المحولة إلى World Space.
- إضافة العرض الحي أو التراكمي، وثلاث درجات للكثافة، وحجم نقاط متغير.
- إضافة التلوين حسب المسافة أو الثقة وخيار الثقة المرتفعة فقط.
- تفعيل وظيفة **تصنيف الأسطح** باستخدام `meshWithClassification`.
- إضافة ألوان وفلاتر للجدران والأرضيات والأسقف والطاولات والمقاعد والنوافذ والأبواب.
- إضافة عرض شبكي وشفافية وعدادات ونسبة تصنيف.
- إضافة `SceneKit.framework` للمشروع.
- تحديث الإصدار إلى `0.5.0` والبناء إلى `5`.

## 0.4.0

- فصل مرجع الجاذبية عن موضع العنصر بالكامل.
- إضافة محورين متصلين في مركز الشاشة مشتقين من الجاذبية في World Space.
- إضافة محورين متقطعين مثبتين عند العنصر على الحائط بإحداثيات ثلاثية الأبعاد.
- حساب اتجاه الشاشة بإسقاط محور `worldUp` من ARKit بدل ربط الخط بحواف الهاتف.
- إزالة قلب المحور الأفقي حسب اتجاه كاميرا الهاتف حتى يبقى مرجع الحائط ثابتًا.
- تحسين فلتر الثبات: Dead Zone للحركات الصغيرة واستجابة أسرع للدوران الحقيقي لتقليل التأخير.
- إضافة وظيفة **قياس الزوايا** باستخدام ثلاث نقاط مثبتة في العالم الحقيقي.
- تحديث الإصدار إلى `0.4.0` والبناء إلى `4`.

## 0.3.0

- تثبيت حدود العنصر على مستوى الحائط وتتبعها بصريًا.
- إضافة وضع 4 نقاط واكتشاف المستويات.
- نقل مؤشر قياس المسافة إلى مركز الكاميرا.

## 0.2.0

- إضافة النسخة الأولية من ميزان الميل واختبار ثبات الحساس.

## 0.1.0

- النسخة الأولية من التطبيق.
