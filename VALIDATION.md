# Validation Notes — 0.3.0

## ما تم التحقق منه محليًا

- تحليل Syntax لجميع ملفات Swift وعددها 23 باستخدام Swift frontend.
- صحة YAML لملف `project.yml`.
- نقل مؤشر المسافة إلى مركز `GeometryReader` بدل وضعه داخل تخطيط اللوحات السفلية.
- ربط وظيفة اكتشاف المستويات داخل `LiDARFeature` و`FeatureRouterView`.
- تحديث الإصدار إلى `0.3.0` والبناء إلى `3`.
- مراجعة تحويل نقاط Vision إلى الشاشة وعكس التحويل لإنشاء ملاحظة تتبع.
- مراجعة إسقاط النقاط العالمية إلى الشاشة باستخدام `ARCamera.projectPoint`.

## ما يحتاج GitHub Actions

- Type-check وربط ARKit وRealityKit وVision داخل Xcode.
- التحقق من توفر كل تواقيع APIs المستخدمة على iOS 17.
- إنشاء ملف `.app` وملف unsigned IPA.

## ما يحتاج جهاز Apple فعلي

1. التأكد أن مؤشر المسافة يقع في مركز صورة الكاميرا على iPhone وiPad.
2. مقارنة الخط الأفقي بميزان فعلي مع إبقاء الجهاز ثابتًا ثم تحريكه وترجيجه قليلًا.
3. الضغط على لوحة واضحة ثم تحريك الهاتف يمينًا ويسارًا للتأكد أن الرسم يظل على اللوحة.
4. اختبار تتبع لوحة تتحرك قليلًا على الحائط.
5. اختبار وضع 4 نقاط على شكل لا يملك إطارًا مستطيلًا واضحًا.
6. مقارنة قراءة الميل بميزان رقمي معروف عند 0° و1° و5°.
7. اختبار اكتشاف المستويات في غرفة بها جدار وأرضية وطاولة.
8. اختبار الواجهات على iPhone صغير وiPad وفي الوضعين الرأسي والأفقي المدعومين.

## ملاحظات الدقة

- دقة القياس تعتمد على جودة تتبع ARKit، وضوح حواف العنصر، اكتشاف مستوى الحائط، والمسافة من العنصر.
- الزجاج والأسطح اللامعة والحواف منخفضة التباين قد تحتاج وضع 4 نقاط.
- هذه الأداة مناسبة للضبط العملي والتجربة، وليست بديلًا عن ميزان معاير في الأعمال التي تتطلب شهادة قياس.
