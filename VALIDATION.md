# Validation Notes — 0.13.0

تم تنفيذ الفحوص التالية على مرحلة مركز المراجعة وإعادة مسح الغرف:

- تحليل Syntax لجميع ملفات Swift باستخدام Swift parser.
- التحقق من صحة `project.yml` كملف YAML.
- التحقق من صحة `LiDARLab/Info.plist` كملف Property List.
- التحقق من الإصدار `0.13.0` ورقم البناء `16`.
- التحقق من بقاء Display Name: `3ELiDAR`.
- التحقق من بقاء Bundle Identifier: `com.essam.3E.LiDARLab`.
- التحقق من بقاء URL Scheme: `lidar`.
- التحقق من عدم إضافة Entitlements أو App Group جديد.
- التحقق من استخدام RoomPlan لتصدير USDZ بدل بناء ملف ثلاثي الأبعاد يدويًا.
- التحقق من استخدام `QLPreviewController` لعرض USDZ داخل Apple Quick Look.
- التحقق من وجود مخطط 2D مخصص يعتمد على transforms وdimensions الخاصة بأسطح RoomPlan.
- التحقق من دعم التحريك والتكبير واختيار الغرفة من المخطط.
- التحقق من عدم استبدال الغرفة الحالية عند انتهاء إعادة المسح قبل موافقة المستخدم.
- التحقق من حفظ `original.json` و`candidate.json` وسجل `room-revisions.json`.
- التحقق من قبول المرشح أو رفضه وتحديث حالة سجل المراجعة.
- التحقق من إعادة تسجيل سماكات حوائط الغرفة بعد اعتماد المرشح، مع الاحتفاظ بسجلات الحوائط المستخدمة من الغرف الأخرى.
- التحقق من إعادة تشغيل `StructureBuilder` بعد اعتماد مراجعة غرفة.
- التحقق من تحميل `structure.json` وسجل المراجعات عند فتح مشروع محفوظ.
- التحقق من إضافة زر فتح أحدث مشروع مكتمل.
- التحقق من إلغاء إعادة المسح دون تغيير الغرفة الأصلية.
- التحقق من وجود نسخة احتياطية لملفات 0.12.0 داخل `_backup/RoomScan_phase4`.
- فحص سلامة ملف ZIP النهائي بعد إنشائه.

## قيود التحقق

لا تتوفر Apple iOS SDKs أو جهاز LiDAR داخل بيئة التنفيذ الحالية، لذلك يتبقى:

1. تشغيل GitHub Actions على macOS للتأكد من Type-check والربط الكامل مع RoomPlan وQuickLook وSwiftUI.
2. اختبار اتجاه مخطط 2D على أجهزة iPhone وiPad وفي الاتجاهات المدعومة.
3. اختبار Quick Look للمبنى المجمّع ولغرفة منفردة.
4. اختبار إعادة مسح غرفة ثم رفض المرشح والتأكد من بقاء النتيجة القديمة.
5. اختبار إعادة المسح ثم قبول المرشح والتأكد من تحديث `room.json` و`structure.json`.
6. اختبار إعادة مطابقة الحوائط المشتركة بعد استبدال غرفة.
7. إغلاق التطبيق بعد وجود مراجعة معلقة ثم فتح المشروع والتحقق من استعادة المقارنة.
8. فتح مشروع مكتمل بعد إعادة تشغيل التطبيق، ثم إعادة تحديد الموقع وبدء إعادة المسح.

## سيناريو الاختبار الأساسي على الجهاز

1. امسح غرفتين وأنهِ المبنى.
2. افتح **مركز المراجعة 2D و3D**.
3. حرّك مخطط 2D وكبّره واختر كل غرفة بالضغط على حائط.
4. افتح عرض 3D للمبنى ثم لغرفة محددة.
5. اختر غرفة واضغط **إعادة المسح**.
6. امسح الغرفة واضغط **إنهاء للمقارنة**.
7. راجع أعداد الحوائط والأبواب والنوافذ في النسختين.
8. ارفض النتيجة وتأكد أن المخطط القديم لم يتغير.
9. أعد المسح مرة ثانية واعتمد النتيجة الجديدة.
10. تأكد من تحديث المخطط وملفات المراجعة والنموذج المجمّع.
11. أغلق التطبيق وافتحه، ثم اضغط **فتح أحدث مشروع مكتمل** وتأكد من ظهور سجل المراجعات.


## Hotfix 0.13.1

- Removed duplicate top-level `QuickLookPreview` declaration from `ThreeEURLRouter.swift`.
- Kept the shared implementation in `LiDARLab/Shared/QuickLookPreview.swift`.
- Parsed all Swift source files with `swiftc -frontend -parse`.

## Phase 5C/5D — 0.14.0

- تحليل Syntax لجميع ملفات Swift، بما فيها ملفات التصحيح والفتحات وSceneKit الجديدة.
- التحقق من صحة `project.yml` و`Info.plist` وكل ملفات JSON داخل المشروع.
- التحقق من وجود تعريف واحد فقط لـ `QuickLookPreview`.
- التحقق من بقاء Bundle ID وURL Scheme والتوقيع دون تغيير.
- التحقق من حفظ الأجزاء المكملة كملفات مستقلة وعدم استبدال `room.json` الأصلي.
- التحقق من حالات قبول ورفض الجزء المكمل وتحميلها بعد فتح المشروع.
- التحقق من حفظ العناصر اليدوية وحالات إخفاء نتائج RoomPlan في ملف مستقل.
- التحقق من ارتباط العنصر اليدوي بـ `buildingWallID` وإحداثيات عالمية ثابتة.
- التحقق من عرض التصحيحات والعناصر اليدوية في 2D وSceneKit 3D.
- التحقق من استمرار Quick Look كعارض للـ USDZ الأصلي.
- التحقق من إضافة نسخة احتياطية لملفات 0.13.1 داخل `_backup/RoomScan_phase5`.

### الاختبارات المطلوبة على جهاز LiDAR

1. بناء النسخة عبر GitHub Actions باستخدام Xcode 16.4 أو أحدث.
2. فتح مشروع مكتمل، اختيار غرفة، ثم مسح جزء ناقص واعتماده.
3. التأكد من ظهور الجزء المكمل بخط بنفسجي في 2D وبشفافية في 3D.
4. اختيار حائط وإضافة باب يدوي وربطه بالغرفة المجاورة.
5. تعديل الباب ثم حذفه والتأكد من تحديث `manual-openings.json`.
6. إخفاء باب اكتشفه RoomPlan ثم إعادة إظهاره.
7. إغلاق التطبيق وإعادة فتح أحدث مشروع والتأكد من عودة التصحيحات والعناصر اليدوية.
8. إعادة مسح غرفة تحتوي عناصر يدوية والتحقق من العناصر التي تحتاج إعادة ربط يدويًا.

لا تتوفر Apple iOS SDKs داخل بيئة التنفيذ الحالية، لذلك لم يتم تنفيذ Type-check أو Link كامل ضد RoomPlan وSceneKit؛ تم تنفيذ فحص Syntax وبنية الملفات فقط.

## Phase 5E/5F — 0.15.0

- تحليل Syntax لجميع ملفات Swift باستخدام `swiftc -parse`.
- Type-check مستقل لنماذج التصحيح ومحرك الفحص باستخدام وحدات اختبار بديلة لـ RoomPlan وsimd.
- تشغيل سيناريو محرك فحص تجريبي والتأكد من اكتشاف تعارض السماكة والحالة غير المؤكدة.
- التحقق من حفظ هندسة الحوائط في ملف مستقل وعدم تعديل `CapturedRoom`.
- التحقق من استخدام التعديلات في مخطط 2D وعارض SceneKit 3D.
- التحقق من إعادة حساب موضع الفتحات اليدوية بعد تعديل الحائط.
- التحقق من استعادة ملفات التعديلات عند فتح المشروع المحفوظ.
- التحقق من الإصدار `0.15.0` ورقم البناء `19`.

### الاختبارات المطلوبة على جهاز LiDAR

1. تشغيل GitHub Actions والتأكد من Type-check والربط مع Apple SDKs.
2. فتح مشروع غرفتين واختيار حائط مشترك ثم تعديل طوله وموقعه.
3. التأكد من تحرك الوجهين في 2D و3D مع بقاء ارتفاع كل غرفة مستقلًا.
4. إضافة باب يدوي، تعديل الحائط، والتأكد من بقاء الباب على موضعه النسبي.
5. إنشاء تعارض سماكة مقصود والتأكد من ظهوره في تبويب المشكلات.
6. استخدام «تثبيت على اتجاه المبنى» ومراجعة النتيجة قبل الحفظ.
7. إغلاق التطبيق وفتح المشروع والتأكد من استعادة التعديلات والتقرير.
8. استعادة هندسة RoomPlan الأصلية والتأكد من اختفاء التصحيح.


## Phase 5G — 0.16.0

- فحص Parsing لجميع ملفات Swift بعد إضافة نماذج المناسيب ومحررات السقف.
- التحقق من استخدام `CapturedRoom.floors` و`polygonCorners` مع iOS 17.
- التحقق من حفظ واستعادة `Review/room-levels.json`.
- التحقق من تمرير بيانات المناسيب إلى مخطط 2D وعارض SceneKit.
- التحقق من الإصدار `0.16.0` ورقم البناء `20`.
