# Validation Notes — 0.7.0

تم تنفيذ الفحوص التالية:

- تحليل Syntax لكل ملفات Swift باستخدام Swift 6.2 بوضع parser.
- عدد ملفات Swift في المشروع: 43.
- التحقق من صحة `project.yml` وتحويله إلى YAML صالح.
- التحقق من الإصدار `0.7.0` ورقم البناء `8`.
- التحقق من تفعيل بطاقتي تسجيل الجلسات ومركز التصدير داخل Router والقائمة.
- فحص إنشاء مجلدات التخزين الموحدة: Captures وRooms وRecordings وExports.
- التحقق من تطابق SHA-256 لملفي ميزان الميل مع v0.6.1؛ لم يتغيرا.
- فحص سلامة الحزم المضغوطة بعد إنشائها.

يتبقى تشغيل GitHub Actions باستخدام Xcode 16.4 وiOS SDK 18.5 للتأكد من Type-check والربط الكامل، ثم اختبار التسجيل وحجم الملفات والأداء على جهاز LiDAR فعلي.
