# Validation Notes — 0.10.0

تم تنفيذ الفحوص التالية على مرحلة سماكات الحوائط المشتركة:

- تحليل Syntax لجميع ملفات Swift وعددها 49 باستخدام Swift 6.2 parser.
- التحقق من صحة `project.yml` كملف YAML.
- التحقق من صحة `LiDARLab/Info.plist` كملف Property List.
- التحقق من الإصدار `0.10.0` ورقم البناء `13`.
- التحقق من بقاء Display Name: `3ELiDAR`.
- التحقق من بقاء Bundle Identifier: `com.essam.3E.LiDARLab`.
- التحقق من بقاء URL Scheme: `lidar`.
- التحقق من عدم إضافة App Group entitlement أو تغيير مسارات التخزين الحالية.
- التحقق من أن التعديلات محصورة في وظيفة RoomScan والوثائق والإصدار فقط.
- التحقق من استمرار استخدام `ARSession` واحدة مشتركة بين جلسات الغرف.
- التحقق من عدم تعديل كائنات `CapturedRoom` أو ملفات `room.json` الأصلية.
- التحقق من إنشاء ملف مستقل `wall-metadata.json` للمبنى.
- التحقق من إنشاء `wall-properties.json` لكل غرفة مجمدة.
- التحقق من أن الحائط المشترك يستخدم `buildingWallID` واحدًا في الغرف المرتبطة.
- التحقق من أن تعديل سجل حائط مشترك يحدث ملفات الغرف المرتبطة.
- اختبار منطقي اصطناعي لخوارزمية المطابقة:
  - قبول حائطين متوازيين ومتداخلين بفاصل 25 سم.
  - رفض حائطين متعامدين.
  - رفض حائطين متوازيين بلا تداخل أفقي.
  - رفض حائطين يفصل بينهما أكثر من الحد المقبول.
- فحص سلامة ملف ZIP النهائي بعد إنشائه.

## ما يتبقى

يتبقى تشغيل GitHub Actions على macOS للتأكد من Type-check والربط الكامل مع Xcode وRoomPlan، ثم الاختبار الفعلي على جهاز LiDAR بغرفتين أو أكثر.

الاختبار الأهم على الجهاز:

1. مسح الغرفة الأولى بسماكة 25 سم.
2. مسح الغرفة الثانية بسماكة افتراضية مختلفة مثل 15 سم.
3. التحقق من أن الحائط المشترك يرث 25 سم.
4. تغيير الحائط المشترك إلى 35 سم.
5. التحقق من تحديث نفس `buildingWallID` في ملفي الغرفتين.
