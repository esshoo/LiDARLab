# v0.5.1 — Xcode Build Isolation Fix

هذا الإصدار لا يضيف أي ميزة جديدة؛ هدفه أن تكون إعادة بناء Stable Core منفصلة فعليًا عن Runtime سلسلة v0.4.x.

## الإصلاحات

- حذف مجلد `Features/المسح_الموحد` من المشروع الكامل النظيف.
- استبعاده صراحةً من XcodeGen حتى لا يعود إلى Target عند تطبيق Patch على مشروع أقدم.
- تقسيم حساب زوايا شكل الهاتف في `StableMapView` إلى تعبيرات SIMD صغيرة ومحددة النوع لتفادي توقف type-checker.
- إضافة `return` صريح لكل حالة داخل `trackingReasonCode`.
- رفع الإصدار إلى `0.50.1` ورقم البناء إلى `51`.

## ما لم يتغير

- Location Core وسياسة تسجيل Pose أولًا.
- صيغة الجلسة الخام.
- RoomScan: لم يتغير أي ملف.
- ComputerBridge الأصلي من v0.3.1: لم يتغير أي ملف.
- Windows Receiver داخل الحزمة: بدون تغيير.
